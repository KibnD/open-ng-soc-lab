# Dashboards

No dashboard export is published yet. Private saved objects may contain index patterns, identifiers, topology, and version-specific fields. A public dashboard becomes eligible only after sanitization, synthetic-data testing, export/import validation on the stated Wazuh version, provenance review, and screenshots free of sensitive metadata.

Dashboard availability is `target`; the current README architecture diagram and detection coverage matrix are the supported public views.
