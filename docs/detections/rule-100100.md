# Rule 100100 — MISP-enriched suspicious SSH source

| Field | Value |
|---|---|
| Data source | Wazuh SSH alert plus MISP lookup result |
| Expected decoder | `json` |
| Expected level | 12 |
| Wazuh baseline | 4.8.2 |
| Evidence | `tested-private` (PT-02 PASS) |
| ATT&CK | No standalone mapping; CTI enrichment is context |

PT-02 replayed one synthetic `sshd` line using documentation address `198.51.100.42`. The source first matched Wazuh rule 5710, then enrichment produced rule 100100, the Shuffle workflow finished, and Slack returned HTTP 200. Observed enrichment latency was 3.853 seconds for this single test; it is not an SLA.

Public tests require synthetic match, no-match, timeout, invalid JSON, and server-error responses. Investigate IOC provenance/confidence, first/last seen, SSH outcome, target exposure, and related activity. Never block solely on an unqualified feed match. Remove the synthetic IOC and disposable webhook after testing.
