# Rule 100051 — Priority 1–2 Suricata alert

| Field | Value |
|---|---|
| Data source | Suricata EVE forwarded through pfSense Syslog |
| Expected decoder | `pfsense-suricata` |
| Expected level | 8 |
| Wazuh baseline | 4.8.2 |
| Evidence | `tested-private` |
| ATT&CK | Signature-dependent; no universal mapping |

The private rule selects Suricata priorities 1 and 2. A priority-1 BlackSun signature matched; priority 3 was excluded. Exact XML and decoder export remain pending.

**Positive test:** sanitized priority-1 or priority-2 EVE event. **Negative test:** equivalent priority-3 event. Expected public result is unavailable until `wazuh-logtest` reproduction.

False positives depend on the originating signature. Investigate signature metadata, source/destination, affected asset, related traffic, and endpoint evidence. Tune the Suricata signature or a narrowly scoped Wazuh exception, never the entire priority class. Contain only after corroboration. Replayed telemetry changes no system state.
