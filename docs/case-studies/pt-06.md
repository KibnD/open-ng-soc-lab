# PT-06 — Privileged container launch

**Status:** PASS with private evidence and cleanup. **Public reproduction:** pending fixtures.

The controlled command launched a privileged container with no network, no mounts, and `true` as its workload. Audited sudo telemetry matched rule 100056. The disposable container was removed and cleanup verified.

This proves detection of the command pattern; it does not demonstrate successful container escape.
