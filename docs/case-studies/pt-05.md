# PT-05 — Windows event-log clearing

**Status:** **NOT RUN**.

The live action was intentionally rejected because clearing Windows logs destroys evidence. Rule 100052 has separate earlier private validation, but PT-05 itself must never be described as executed or passed.

Future public validation must use a synthetic fixture and `wazuh-logtest`; it must not clear operational logs.
