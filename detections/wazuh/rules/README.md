# Wazuh Rules

No rule XML has been copied from the private laboratory yet.

Only minimized original rules with documented parent dependencies, provenance, supported version, and deterministic positive/negative tests may be added here. Candidate IDs are tracked in [`detections/tests/manifest.json`](../../tests/manifest.json).
