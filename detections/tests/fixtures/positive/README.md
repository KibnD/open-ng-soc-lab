# Positive Fixtures

Store one minimized sanitized input per expected detection. Never copy a full log, EVTX, alert index, PCAP, screenshot, or unredacted VM export.

Each fixture must have an expected result naming the decoder, custom rule ID, and alert level.
