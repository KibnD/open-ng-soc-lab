# Negative Fixtures

Store meaningful near-match events that must decode but must not trigger the target custom rule.

Examples include non-privileged AD groups, AES rather than RC4 service tickets, standard rather than privileged containers, and low-priority Suricata informational events.
