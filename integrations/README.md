# Integrations

Integration content is deferred until its release milestone and security gate:

- MISP: v0.2.0 after credential rotation, source review, and synthetic tests.
- Shuffle/Slack: v0.3.0 after rebuilding without authentication objects, webhook identifiers, channel IDs, or execution history.

No live `.env`, API response, workflow export, authentication object, or secret belongs in this repository.
